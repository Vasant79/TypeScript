"use strict";
/**
 *
 * @param id is of type string and number
 * lets you describe how a thing should look
 * types - lets to do | and &
 */
function getId(id) {
    console.log(id);
}
function getId2(id) {
    console.log(id);
}
getId(32);
getId("vasu79");
